# hypridle — lock after 5 min, then suspend after 20
listener {
    timeout = 300
    on-timeout = hyprlock
}

listener {
    timeout = 1200
    on-timeout = loginctl suspend
    # only suspend if on battery; harmless on AC
}
